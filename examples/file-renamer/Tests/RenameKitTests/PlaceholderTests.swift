import XCTest

// Canary so the test target has sources from Task 1; real MUST suites land in Tasks 2-4.
final class PlaceholderTests: XCTestCase {
    func testPackageBuilds() { XCTAssertTrue(true) }
}
